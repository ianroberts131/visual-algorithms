
import * as actions from './src/actions/actionCreators';

/*
describe('actionCreators', () => {
  it('Should Create an Action to searchBaseState', () => {
    expect(actionCreators.sortBaseState).toEqual({
      type: types.SORT_BASE_STATE
    })
  })
*/
